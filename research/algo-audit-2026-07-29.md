███ LENS: LENS 4 — is the geography itself biased?
HEADLINE: Yes, and the mechanism is (b), not (a), (c) or (d): the K=8.0 bucket ceiling is CLASS-STRATIFIED. True effective competitor weight is ~13.6 suburban vs ~25.3 urban (calibrated lower bounds 23 / 43) — both stored as 8.0 — so the clip understates urban competition 1.86x more than suburban. Removing it alone moves urban/suburban from 1.94x to 0.73x (62% of the gap) and flips the metro top-50 from 50/50 urban to 0/50 urban with ZERO overlap. Worse for the buy-box: in 83% of suburban and 100% of urban cells the competitor distribution is a single point mass, so competition carries no information there — share at a fixed entrant size varies only 1.53x across suburban cells vs 5.59x rural. "High-income underserved suburb" is not missing from the output, it is UNREPRESENTABLE: 20% share needs 88 modelled DVMs at true suburban K, and the best share among all 2,845 high-income suburban/exurban cells is 5.1%.

  [CRITICAL] The K=8.0 clip is class-stratified — it is the urban-over-suburban bias, and it alone decides the metro top-50
    evidence:  Reproduced per-class fit (n=525 GP clinics, replica tracks shipped gate: replica quantile-ratio p50 1.066 vs gate 1.093): mean modelled/actual = rural 0.19x, exurban 0.31x, suburban 0.91x, urban 1.76x — urban/suburban 1.94x, spread 9.1x. Demand-weighted mass in the clipped top bucket, by class (median): rural 4.9%, exurban 71.0%, suburban 97.1%, urban 99.8%. Estimated true K at cell centre (p50): rural 1.3, exurban 4
    mechanism: build-catchments.mjs `covHH[band][Math.min(KBUCKETS-1, Math.round(K/KSTEP))] += w` with KBUCKETS=17, KSTEP=0.5. K is a SUM over every competitor reaching a household, so it scales with clinic density (100 clinics within 
    fix:       Do not raise KBUCKETS alone — verified catastrophic: per-cell true K on its own drops every class to 0.13-0.20x (pooled p50 1.07->0.19) because CATCH_MAX_BAND=5 was chosen empirically to offset this clip, exactly as CLAUDE.md documents. The clip and the band cap must be re-derived JOINTLY, and the j

  [CRITICAL] In suburbs and cities the competitor distribution is a single point mass — the model has no competitive resolution where the buy-box lives
    evidence:  Median mass in the single largest cd[0] bucket: rural 36.6%, exurban 57.4%, suburban 100.0%, urban 100.0%. Share of cells that are >95% one bucket: rural 2%, exurban 21%, suburban 83%, urban 100%. Consequence, measured at a FIXED 2.5-DVM entrant so only competition can move the number: capture share p10/p50/p90 = rural 8.02/17.02/44.84% (p90/p10 = 5.59x), exurban 2.23/3.06/5.88% (2.63x), suburban 1.99/2.47/3.05% (1.5
    mechanism: Same line as above, but this is the information-theoretic consequence rather than the level error: once >95% of a cell's households land in one bucket, `_catchShareBand`'s sum over cd[i] collapses to a single term A/(A+w
    fix:       This is not fixed by a bigger ceiling alone — it needs the bucketed histogram replaced with a representation that stays informative in dense markets (e.g. ship per-band mean and variance of K, or log-spaced buckets, both build-side changes in build-catchments.mjs). Until then, the honest move is to 

  [CRITICAL] The buy-box query is arithmetically unsatisfiable, so 'zero cells found' is not evidence about the market
    evidence:  Of 2,845 suburban/exurban cells with catchment income >= $100k: 0 clear 20% share, 0 clear 15%, 0 clear 10%, 0 clear 7.5%, exactly 1 clears 5%. Best share in the entire set 5.1%; median 1.6%. Structural solve of share = A/(A+wBar*K) with A = sqrt(n/2.5) for the roster n needed to reach 20% share: at the clipped K=8 every class needs ~10-11 DVMs; at calibrated true K, suburban needs 88 DVMs and urban 307. Top-20-by-re
    mechanism: The trade area is a uniform 20-minute drive budget, but K sums EVERY competitor inside it. Competitor count scales with density (rural 8, exurban 39, suburban 100, urban 166 within 25 min) while the budget does not, so s
    fix:       Stop asking this model the underservice question. The quantity the buy-box actually needs — local demand per effective competitor — already exists in the Opportunity List as `_under` = dogHH/(dvm+1) at ZIP grain, and CLAUDE.md already flags that the list and the eval can disagree on screen. The corr

  [MAJOR] The fit gate is a distribution match, and its statistic is structurally blind to class stratification
    evidence:  research/eval-gate.mjs section 9 computes ratio[p] = quantile(model,p)/quantile(roster,p) — quantile of one population over quantile of another, not the distribution of per-clinic errors. On the same 525-clinic sample: gate statistic p25 0.978 / p50 1.066 / p75 1.000 / p90 1.122 (passes 1.09 +/- 0.20), while the distribution of per-clinic ratios is p25 0.366 / p50 0.841 / p75 2.057 / p90 3.927. The modelled quantiles
    mechanism: Comparing marginal quantiles cannot detect a permutation of the assignment: any model reproducing the marginal spread of roster sizes passes, including one that assigns those sizes at random. The pooled form additionally
    fix:       Add `fit.roster.byclass` next to fit.roster: run the identical quantile statistic within each of the four `_catchUrbanClass` buckets, with per-class baselines and the same +/-0.20 tolerance, and FAIL on any class outside it (record today's 0.19/0.37/1.20/1.80 as a known-bad baseline so the check tra

  [MINOR] The uniform 20-minute credit does truncate sparse catchments more — confirmed, monotone, but small for the buy-box
    evidence:  Band-5 (20-25 min) demand as a share of full 25-min demand, median by class: rural 55.4%, exurban 51.6%, suburban 44.7%, urban 40.1% — monotone, as hypothesised. Extending credit to 25 minutes multiplies mean modelled size by rural 2.360x, exurban 1.676x, suburban 1.481x, urban 1.319x — also monotone, and a bigger differential than the demand shares alone imply because rural cd is unclipped in the outer bands. But th
    mechanism: CATCH_MAX_BAND=5 in index.html drops band 5 for every cell. A sparse catchment carries proportionally more of its households in the outer rings (rural a25/a20 = 1.89 with 55% of demand beyond 20 min), so a constant time 
    fix:       Make the credit horizon follow the urbanicity class the model already computes, the same way the decay curve theta does (CATCH_URBANICITY already carries per-class beta/theta; the band cap is the one geography parameter still held constant). This cannot be changed independently — CATCH_MAX_BAND=5 is

  [MINOR] CLEARED — the band-0 urbanicity classifier is NOT the urban-over-suburban mechanism (but it is a real, user-visible labelling defect)
    evidence:  Tested and refuted as a bias source: reclassifying on 20-minute household density instead of band-0 moves urban/suburban the WRONG way, 1.94x -> 2.41x (spread 9.1x -> 13.0x), so the current classifier is if anything suppressing the gap. Also cleared: per-class decay curves are not the mechanism either — forcing all classes onto the suburban W curve gives 2.26x, onto the rural curve 1.94x, both >= shipped. Hypothesis 
    mechanism: `_catchUrbanClass` computes household density from cell.b[0] (the 8-minute ring) over cell.a8 only. On a density gradient the inner ring does not describe the catchment. The resulting class is user-visible: the metro-run
    fix:       Do not change the classifier as a bias fix — it is measured to make the bias worse. Treat it as a separate labelling correction: either widen the density basis to the credited bands, or (cheaper and lower-risk) stop printing the raw class name in the metro banner where a 111-hh/sq-mi Celina cell rea

  [MAJOR] Modelled practice size is nearly flat in actual practice size within a class — the rho warning understates the problem
    evidence:  Within suburban, modelled size by actual roster: 2.71 DVM for clinics that really have 1, 2.63 for 2, 2.23 for 3-4, 2.55 for 5+ — non-monotone and essentially constant. Within urban: 5.40 / 6.63 / 5.20 / 6.58. Spearman(modelled, actual) = -0.017 pooled (gate reports -0.026 on its own n=569), and within-class rural -0.066, exurban -0.048, suburban -0.116, urban +0.054. Under the per-cell true-K counterfactual it does 
    mechanism: Downstream of the previous two items. Once cd is a point mass, share is constant within a class and the equilibrium solve reduces to a function of reach, which varies smoothly across a metro — so the output separates cla
    fix:       Do not attempt a per-site accuracy claim until competition is representable at cell grain (item 2). In the meantime the site card and the print report should carry the market-level/firm-level distinction explicitly — CLAUDE.md already records corr 0.002 per clinic vs 0.697 at market level, but the U

  CLEARED: CLEARED — the per-class bias is not an artefact of the roster sample

███ LENS: Objective function — is maximising winnable revenue the right thing to rank for a PE entrant?
HEADLINE: The math is largely fine; the objective is wrong — and it is wrong in a way no re-weighting can fix. Ranking on total winnable revenue ranks MARKET SIZE (reach explains 81.4% of log-revenue variance), and a super-linear fixed point (elasticity 1.9 measured, vs 0.0 in actual rosters) amplifies that. Every one of the 3,224 high-income suburban/exurban cells is strictly Pareto-dominated on BOTH (reach, share), so the 34-cell frontier is 9 urban + 25 rural with zero suburbs: no scalar of the two current axes can ever surface the buy-box. The fix is structural — screen first, cap the size term at a buildable practice, and turn on the growth axis that is already measured and deliberately unscored. A screen-then-score prototype returns Keller / Frisco / McKinney / Prosper / N Richland Hills at mean $138k income, 10 suburban + 6 exurban + 4 urban + 0 rural.

  [CRITICAL] The buy-box is Pareto-unreachable: no monotone objective on (reach, share) can surface a high-income suburb
    evidence:  obj1/obj2.mjs, 9,627 DFW cells. Exact additive decomposition of log(revenue): REACH 81.4%, SHARE 22.6%, PRICE -1.4%. Spans p95/p05: reach 72.9x, share 29.2x, price 1.28x. Sweeping the frontier by descending reach, only 34 cells are non-dominated — 9 urban + 25 rural, ZERO suburban/exurban. All 3,224 cells that are (suburban|exurban) AND band-0 income >= $100k are strictly beaten on BOTH axes (100.0%). Fine sweep of r
    mechanism: index.html:9077 `g.score = (g.winRev/CATCH_REV_FULL)*accessMult*retailBonus` with `g.winRev = g.cEq*DVM_VISIT_CAPACITY*REV_PER_VISIT*(g.cACT||1)` (9075-9076). Revenue per doctor is EXACTLY DVM_VISIT_CAPACITY x REV_PER_VI
    fix:       Stop ranking a scalar product of size and winnability. Screen, then score, which is what every documented site-selection playbook does (research/vet-site-selection-report.md: Esri/Buxton screen-then-score funnel) and what the app's own Opportunity List already does with `_util`. Concretely: (1) SCRE

  [CRITICAL] _catchEquilibrium is super-linear in demand (elasticity ~1.9) while real practice size is flat in market size
    evidence:  obj4.mjs controlled experiment — take ONE real cell, multiply only its demand vector by k, resolve: local d log(n)/d log(reach) = 1.89, 1.92, 1.80, 1.78 on four real cells spanning exurban 55k to urban 578k visits. Within-class regressions: urban 1.63, suburban 1.62, exurban 1.05, rural 0.46. Against 525 GP clinics with scraped rosters: ACTUAL rosters regressed on reach give slope -0.033 — real practices are the same
    mechanism: index.html:7975 `_catchAttract = n => Math.sqrt(Math.max(0.05,n)/2.5)` inside the fixed point at 8133-8138 `_catchWinnable(cell,dem,mid,wBar) > mid*DVM_VISIT_CAPACITY`. In the competitive regime (median band-0 competitor
    fix:       Sweep the attraction exponent P in `_catchAttract = n => (max(0.05,n)/2.5)**P`. Measured across all 9,627 cells and the 525 roster pairs: P=0.50 (shipped) elasticity 1.86, class ratios 1.76/0.91/0.31/0.19, Q-Q p50 1.07. P=0.25 elasticity 1.30, ratios 1.33/0.88/0.42/0.25. P=0 (fixed reference entrant

  [MAJOR] The only quantitative gate on the capture model is pairing-blind, so it certifies a model with zero per-site skill
    evidence:  research/eval-gate.mjs:904 computes `out.ratio[k] = mq/rq` — quantile of modelled / quantile of roster. Shipped run: fit.roster PASS, n=569, p25 1.253 p50 1.093 p75 0.954 p90 0.755; fit.rho WARN at Spearman -0.026. Reproduced independently (obj5.mjs, n=525): Q-Q p25 0.98 / p50 1.07 / p75 1.00 / p90 1.12, Spearman -0.041. Randomly permuting the modelled values across clinics gives BIT-IDENTICAL quantile ratios (0.98 /
    mechanism: A quantile-ratio test constrains the MARGINAL distribution only. Any model that draws the right histogram passes it regardless of which value it assigns to which site. RHO_FLOOR=0.10 exists (research/eval-gate.mjs:869) b
    fix:       Add a per-class ratio gate (urban/suburban/exurban/rural modelled-vs-actual, each with its own tolerance) so the mixture can no longer cancel; that single check fails today at urban 1.76 / exurban 0.31. Keep fit.rho as a WARN until the P-sweep lands, then promote it to a FAIL with a floor set from w

  [MAJOR] Growth is measured, differentiates strongly, and is deliberately excluded from the score
    evidence:  `fu[band]` ships per cell in dfw-catchments.json and `cell.cFutTot` is computed at index.html:8023, then rendered at 9623 as 'trajectory only, not in this score'. Measured median n_eq uplift from crediting buildout demand (obj8.mjs, using the shipped CATCH_FUT_VISITS_PER_UNIT 1.62 and CATCH_FUT_CONF 0.5): urban 1.07x, suburban 1.09x, exurban 1.36x, rural 1.49x — and by place Celina 4.08x, Prosper 2.39x, Melissa 2.25x
    mechanism: Site scoring at index.html:9075-9077 uses `g.cEq` (today's demand only). `g.cEqFut` is computed at 8044 but consumed ONLY in the land-play branch (9102-9104), which is additionally gated behind `_lpQual` requiring cFutTo
    fix:       Score sites on a blended or buildout equilibrium — e.g. rank on `_catchEquilibrium(cell, dem + futureDem)` — and disclose the discount on the card exactly as the land-play branch already does. The pipeline is already discounted twice (CATCH_FUT_CONF 0.5 on top of announced-not-built).

  [MAJOR] Income is the only quality axis in the score, and it carries 1.28x of range with the wrong aggregate sign
    evidence:  revenue/DVM = DVM_VISIT_CAPACITY x REV_PER_VISIT x cACT exactly; measured p05 $481,531 / p50 $563,522 / p95 $616,667 = 1.28x span across all of DFW, against a 72.9x reach span. cACT p05 0.781 -> p95 1.000. Net effect: corr(revenue, catchment income) = -0.181 and PRICE contributes -1.4% of log-revenue variance — the model's only income term works against income in the aggregate ranking. Top 20 by the shipped score ave
    mechanism: index.html:7833 `_catchACTF` ramps 0.57 -> 1.00 between $25k and $110k catchment income and then clamps. Everything above $110k reads identically, so Southlake at $184k, Prosper at $162k and a $110k cell are the same pri
    fix:       Two independent moves. (1) Let cACT keep ramping above $110k, or add a separate high-income premium — the empirical ACT gradient does not stop at $110k and 2,260 DFW cells sit above $120k. (2) More importantly, income cannot matter while size is uncapped; it only becomes decisive once the size term 

  [MAJOR] Two screens in the same product disagree by ~3,000 ranks on the flagship market
    evidence:  Opportunity List documented and verified top-6 (CLAUDE.md): Southlake 95 / Prosper 88 / Frisco 84 / Argyle / Flower Mound / Celina. Evaluate-metroplex capture-model best-cell rank for the same places, of 9,627 cells: Southlake #2954 (31st percentile), Prosper #2755, Frisco #924, Celina #4236, Argyle #5234 (46th percentile). The List calls Southlake the #1 market in DFW; the site engine puts its best site below two-th
    mechanism: The List scores crow-flies at ZIP grain with an explicit underservice term (dogHH per DVM, log-transformed, x `_util`); the site engine scores measured drive-time reach x share x price at cell grain with no underservice 
    fix:       Once P1 lands, the site engine's screened ranking is much closer to the List's intent (both then reward defensible high-income markets rather than the biggest one). Until then, surface the disagreement rather than hiding it: when a metro run's best cell in a ZIP falls far below that ZIP's List rank,

  CLEARED: A two-dimensional (size x winnability) frontier, tested as an alternative to the scalar · Shortening CATCH_MAX_BAND to recover the share signal

███ LENS: Lens 2 — why is capture share ~6% everywhere, is that real, and can narrowing the competitive horizon make it discriminate between suburbs?
HEADLINE: Share is not a competitive measurement — it is the identity doctors×3200/reach, so "share ≥ 20% in a suburb" asks for an 18-doctor practice and can never return a cell; separately, the K=8.0 ceiling has censored competition to a constant (suburban coefficient of variation 0.040) in exactly the markets the buy-box targets, and it is the single largest driver of the urban bias. The buy-box targets DO exist in the data — 116 high-income suburban cells with real unmet local demand — and the shipped score ranks all 116 outside its top 500.

  [CRITICAL] Reported capture share is an accounting identity, not a competitive measurement
    evidence:  Verified on all 9,627 cells: share ≡ doctors × DVM_VISIT_CAPACITY / reach, exact to a max relative error of 1.11e-9% (p99 5.5e-10%) on the 9,365 cells with an interior equilibrium solution. The other 262 (2.7%) only differ because they clamp on the solver's [0.05, 40] bounds (exurban 4.3%, rural 5.4% of cells). Consequence: a 20% share requires n ≥ 0.20 × reach / 3200 — in a MEDIAN suburban cell (reach 290,964 visits
    mechanism: index.html `_catchEquilibrium` (line ~8133) solves `win(n) = n × DVM_VISIT_CAPACITY` by bisection, then the caller reports `share = win / cReach`. Substituting the solved condition gives share = n×3200/reach identically.
    fix:       Stop presenting the equilibrium residual as 'share'. Report the competitive read at a FIXED reference entrant (e.g. share evaluated at 3 DVMs, which is measured and does vary — suburban p10/p50/p90 2.2/2.7/3.3% shipped, 0.7/1.2/2.4% unclipped) and report the equilibrium output as what it is: modelle

  [CRITICAL] K is censored to a constant by the 8.0 bucket ceiling in exactly the markets the buy-box targets
    evidence:  Household-weighted, pooled over all cells: 92.3% of band-0 mass sits in the clipped top bucket (band 5: 89.6%). By class, demand-weighted over bands 0-4: urban 98.7% at ceiling, suburban 95.2%, exurban 75.8%, rural 30.4%. Inner ring alone (band 0, 0-8 min): urban 100%, suburban 96.8%. Across the 3,087 suburban cells the recorded band-0 mean K runs p10 7.79 → p90 8.00, coefficient of variation 0.040 — the variable is 
    mechanism: build-catchments.mjs line 565: `covHH[band][Math.min(KBUCKETS-1, Math.round(K/KSTEP))] += w` with KBUCKETS=17, KSTEP=0.5. Every household whose effective competitor weight exceeds 7.75 is recorded at 8.0. index.html `_ca
    fix:       Raise KBUCKETS so the top bucket is empty at the observed maximum (measured max true K ≈ 50, so ~101 buckets at KSTEP 0.5, or keep 17 buckets on a log/quantile scale). This is a build-side change to dfw-catchments.json — the derive pass, ~25 min, no OSRM. Then re-derive CATCH_MAX_BAND (or the entran

  [CRITICAL] The ceiling, not the geometry, is the primary source of the urban over-prediction
    evidence:  The ceiling's inflation is differential by exactly the urbanicity axis the bias runs on: 3.22× urban, 2.03× suburban, 1.05× exurban, 1.00× rural (share at recorded K=8 vs at median true K, unit entrant). Test: run the model on the unclipped cd with ONE free scalar on entrant attraction to restore the level. At mult=3 the roster gate reads p25/p50/p75/p90 = 0.52/1.07/2.14/3.84 versus the shipped 0.37/0.84/2.06/3.93 — 
    mechanism: share = A·W[b] / (A·W[b] + wBar·K). Censoring K at 8.0 removes a fixed amount of competition from every household above the ceiling; the number of such households rises monotonically with density (98.7% urban → 30.4% rur
    fix:       Treat the ceiling and the level as one joint re-derivation, not two independent fixes: un-clip the buckets and re-solve CATCH_MAX_BAND (and/or `_catchAttract`'s 2.5 divisor) against the roster gate. Add a RANK-correlation test to the gate — the quantile gate is a level test and provably cannot see t

  [CRITICAL] The buy-box targets exist in the data and the engine ranks all of them outside its top 500
    evidence:  Measured outside the Huff share entirely, on horizon-matched local underservice (visits reachable in 10 min ÷ DVMs whose OWN 10-min ring covers the point, real scraped rosters, unknown=2): 116 suburban/exurban cells with income ≥$100k exceed 3,200 visits per incumbent DVM; 57 exceed 4,800; 27 exceed 6,400. The top of that list is exactly Jon's description — 75063 Valley Ranch/Las Colinas $105k 5,492 visits/DVM (7.9 D
    mechanism: The objective is expected revenue of the practice the equilibrium supports, `e × CAP × REV_PER_VISIT × _catchACTF`. Because share is the identity in problem 1, revenue reduces to a monotone function of reach — measured r
    fix:       Add a second, separately-reported objective alongside modelled revenue: local unmet demand = (visits reachable inside 10 min) − (DVMs whose own 10-min ring covers the point × DVM_VISIT_CAPACITY), i.e. rank on headroom rather than on market size. It needs no new data — rosters, band demand and clinic

  [MAJOR] Metro closure gap: the demand model supports half the doctors that are physically present
    evidence:  DFW bbox, 302 ZIPs: 2,932,330 households → 4,037,297 modelled vet visits/yr under the app's own pet model. The artifact holds 911 clinic catchments / 2,526 modelled DVMs (615 with scraped rosters averaging 3.14; the 296 unknowns default to 2, which UNDERSTATES the true count). Realised = 1,598 visits per DVM against DVM_VISIT_CAPACITY = 3,200 — ratio 0.50. Restated: the demand model supports 1,262 full-time DVMs; 2,5
    mechanism: Two independently-calibrated halves that are never reconciled: demand is HH × 0.45 dog rate × 2.4 visits + HH × 0.25 × 1.2 (index.html ~7127 dogData build sites), while supply capacity is DVM_VISIT_CAPACITY=3200 derived 
    fix:       Add a metro-closure assertion to research/eval-gate.mjs: total modelled visits ÷ total observed DVMs must land within a stated band of DVM_VISIT_CAPACITY. That single check would have caught this and would catch any future recalibration that fixes one half in isolation.

  [MAJOR] Income is a 1.5× term fighting a 72.9× term that is itself anti-correlated with income
    evidence:  Dynamic range across the 9,627 cells (p95/p05): 20-min reach 72.9×, capture share 29.2×, income→visits factor (_catchSpendF, floor 0.74) 1.2×, income→price factor (_catchACTF, floor 0.57) 1.3×, both combined 1.5×. And income is negatively correlated with reach: r=−0.214 overall, −0.243 within suburban cells only — high income co-occurs with lower household density. This is NOT caused by the competition model and none
    mechanism: index.html `_catchSpendF` (line ~7829) and `_catchACTF` (~7833) are floored linear ramps over $25k-$110k, giving a combined 1.5× swing. They multiply a demand term whose spread is 72.9×. The product is dominated by the d
    fix:       This is an objective-design issue, not a calibration one — do not widen the ramps to force it (that fabricates an income elasticity the AVMA/BLS derivation does not support). Instead score income as a separate reported axis, or filter on it before ranking (which is what the buy-box actually is), rat

  [MINOR] The competitor-strength term wBar is effectively inert across the metro
    evidence:  `wb` across all 9,627 cells: p05 0.951, p50 1.024, p95 1.066 — a 1.12× total spread. It is the ONLY attractiveness axis in the model (W4 was closed and rating was correctly rejected), and after averaging `_staffW` over a median 127-196 competitors it contributes 12% of variation.
    mechanism: build-catchments.mjs line 611 computes `cell.wb` as the household-weighted MEAN of `staffW` over the whole competitor set. `_staffW` is already clamped to [0.8, 1.8]; averaging ~150 draws from that clamp concentrates har
    fix:       Nothing urgent — but do not reach for wBar as a lever when tuning share; it cannot move it. If competitor size is to matter, it has to enter per-band or per-competitor (which is what the cd distribution would allow if it were not clipped), not as a scalar mean.

  CLEARED: Restricting the competitive set to bands 0-1 does not make share discriminate — HYPOTHESIS TESTED AND CLEARED

███ LENS: Lens 3 — why income is negatively correlated with the score. Exact multiplicative decomposition of the capture-model objective (rev = reach × spendF × share × ACT × REV_PER_VISIT, verified to 4e-16 on 9,365 of 9,627 cells; the other 262 clamp at the 0.05-DVM equilibrium floor), then a variance decomposition on logs, conditional-on-size analysis, and ablation of every proposed income fix. Harness reimplements the shipped model from dfw-catchments.json + free Census ACS; constants checked line-by-line against index.html. `node research/eval-gate.mjs` passes (43/1 warn/0 fail) on the unmodified tree. Scripts: /private/tmp/claude-501/-Users-jonathanduhon-Downloads-Vet-App/2851584c-f64b-4774-bf3a-925a1f9f5151/scratchpad/lens3.mjs, lens3b.mjs, lens3c.mjs, lens3d.mjs, lens3e.mjs, _l3head.mjs
HEADLINE: The income terms are not weak — they are irrelevant: 84:1 variance ratio against reach, and every reformulation I tested re-ranks at Spearman 0.985-0.999. The hypothesis in the brief is confirmed, and the sign is a Simpson's paradox — hold market size fixed and income turns POSITIVE (+0.126). But the ranking-on-EBITDA/EV theory is refuted (Spearman 0.9998, a no-op), and the real blocker is elsewhere: the K=8.0 competitor-weight clip saturates for 100% of urban and 95% of suburban households, so the model has literally zero competitive resolution in the buy-box's territory.

  [CRITICAL] The objective is market size, so income cannot matter — 84:1 variance ratio
    evidence:  Variance decomposition on logs (contributions sum to 100% by construction): log REACH 82.2%, log SHARE 22.6%, log spendF −0.8%, log ACT −1.4%. sd(log reach)=1.315 vs sd(log income terms)=0.143 → reach:income variance ratio 84:1. Realized p10-p90 spans: reach 28.9x, share 12.6x, spendF×ACT 1.43x. Ablation — set both income multipliers to 1.0 and re-solve every equilibrium: Spearman vs shipped 0.9881, top-50 overlap 41
    mechanism: index.html:9086 `g.score = (g.winRev/CATCH_REV_FULL)*accessMult*retailBonus`, where winRev = cEq×DVM_VISIT_CAPACITY×REV_PER_VISIT×cACT. I verified score ≡ cEq × cACT / 10 to 4.42e-16. cEq is the solved equilibrium practi
    fix:       Do not tune the income terms — no version of them can move the ranking. Change the objective. Revenue is the right number to REPORT and the wrong number to RANK for a buy-box: it selects the biggest catchment, which in DFW is downtown Dallas. A defensible PE objective is size-normalised (EBITDA per 

  [CRITICAL] K=8.0 competitor-weight clip has zero resolution in urban and suburban DFW — the model cannot see 'underserved'
    evidence:  Fraction of inner-ring (0.5-8 min) households sitting in the CLIPPED top competitor bucket, by urbanicity: urban p50 1.000 / mean 1.000 (n=1253), suburban p50 1.000 / mean 0.951 (n=3077), exurban p50 0.589 / mean 0.522 (n=2806), rural p50 0.000 / mean 0.035 (n=2229). So for 4,330 of 9,365 cells (46%) essentially every household is pinned at the same competitor weight regardless of whether it faces 2 clinics or 20. Se
    mechanism: build-catchments.mjs:488 `const KBUCKETS=17, KSTEP=0.5` → max representable effective competitor weight is 8.0; line 565 `covHH[band][Math.min(KBUCKETS-1, Math.round(K/KSTEP))] += w` folds everything above 8.0 into one b
    fix:       CLAUDE.md already documents the clip and its ~2.05x share inflation, but documents it as UNIFORM. It is not — it is class-structured, and the class it saturates is exactly the buy-box's. Raise KBUCKETS to cover the observed distribution (a rebuild of dfw-catchments.json, ~25 min derive, no OSRM), th

  [MAJOR] The $110k saturation ceiling erases the price advantage of exactly the cells the buy-box wants
    evidence:  Both curves reach 1.0 at catchment income $25,000+$85,000 = $110k. 2,757 of 9,365 cells (29.4%) sit AT the ceiling, spanning a real income range of $110k–$183k scored identically. The clip is systematically anti-suburban: 50.1% of exurban cells, 24.6% of suburban, 23.1% of rural — but only 6.2% of urban (urban median catchment income is $87k, comfortably under the cap). The ceiling removes 57% of ACT variance (sd 0.0
    mechanism: index.html:7830 `_catchSpendF` and :7834 `_catchACTF` — the `Math.min(1, ...)` in `Math.max(0.74, Math.min(1, 0.74+0.26*(inc-25000)/85000))` and the 0.57/0.43 twin. The anchors are the BLS bottom/top quintile MEANS, so a
    fix:       A constant-elasticity form through the SAME two anchors invents no third constant (exponents are forced: visits e=0.2032, price e=0.3794) and does not saturate — a $183k catchment then earns 1.21x on price instead of 1.00x. But I tested it and it is nearly a wash overall (sd(log combined) 0.1425 → 0

  [MAJOR] EBITDA margin is invariant to the local price level, so income never reaches the number PE underwrites
    evidence:  Ranking on EV with the shipped ACT-blind margin is a NO-OP: Spearman vs revenue 0.9998, top-50 still 50/50 urban, income corr only −0.181 → −0.145. Making margin a function of ACT using only constants already in the file changes that materially: direct-costs-only lower bound (AAHA midpoint D=0.21 nationally priced, labor scaling with local wages) gives income corr −0.086, Spearman 0.9796; the all-costs-fixed upper bo
    mechanism: index.html:1782 `_ebitdaMargin(dvm)` and :1795 `_ebitdaMultiple(dvm)` take practice size only. The AAHA/VMG structure they are derived from is stated as SHARES OF REVENUE at an implicit reference price — but direct costs
    fix:       Make _ebitdaMargin a function of (dvm, cACT). The conservative form margin = 1 − L − D/ACT, with D = the AAHA direct-cost midpoint already cited in the code comment and L = 1−m0−D, introduces no new number. This is the correct version of the brief's hypothesis: the error is not 'rank on revenue inst

  [MAJOR] `share` is not a trade-area share — its denominator is the whole 20-minute pool, which is why the buy-box reads as empty
    evidence:  The inner band is only 6.6% of the reach denominator at the median cell (p10 3.0%, p90 11.4%). Three definitions, same numerator, p50 by class — shipped / inner-ring / decay-weighted: urban 0.032 / 0.135 / 0.145; suburban 0.023 / 0.100 / 0.108; exurban 0.015 / 0.063 / 0.069; rural 0.107 / 0.199 / 0.255. The shipped value is a near-constant ~1/4.2 of the inner-ring value. Consequence: max suburban shipped share across
    mechanism: index.html:9095 `g.share = g.cShare`, computed as winnable ÷ cReach where cReach sums every visit out to CATCH_MAX_BAND (20 min). Since the 2026-07-28 redesign moved decay into the share (CLAUDE.md, load-bearing rule #1)
    fix:       Report and gate on a decay-consistent share (winnable ÷ Σ_b dem_b·W_b, or the inner-ring share where W=1 by construction). Note the inner-ring numbers land right on the gravity engine's documented scale — CLAUDE.md records gravity as 'contested corridor ~0.15-0.20, dense suburb ~0.10' against inner-

  [MINOR] Capture mode has no defensibility gate at all
    evidence:  2,226 of 9,365 cells pass `score >= EVAL_STRONG`; only 15 of them would also pass gravity mode's `share >= 0.24`. Mean catchment income of passers is $90k vs $97k for all cells, so the gate is mildly anti-income. That said, I tested the fix and it does NOT rescue the buy-box on its own: gating on inner-ring share ≥0.20 leaves the top 50 at 46 urban / 4 rural, because the top-revenue urban cells already carry the metr
    mechanism: index.html:8169 `_evalStrongOK` — the capture branch is `g.score>=EVAL_STRONG && (!evalDataOk.retail || g.ret>=EVAL_RET_MIN)` while the gravity branch additionally requires `g.demandN>=0.45 && g.share>=0.24`. The share f
    fix:       Restore a defensibility floor on the decay-consistent share once that definition exists. Worth doing for correctness, but it is downstream of the K-clip fix — while share saturates at K=8.0 the gate would be filtering on a saturated signal.

  [MINOR] One published decomposition applied at two different grains
    evidence:  The 0.36 = 0.85 × 0.74 × 0.57 decomposition documented at index.html:7816 is split across grains: _incDampF (the 0.85 ownership term) is applied PER ZIP inside _catchVisits, while _catchSpendF (0.74) and _catchACTF (0.57) are applied ONCE to a catchment-averaged income. I tested moving all three to ZIP grain: it makes things slightly worse, not better — corr(revenue, income) −0.178 → −0.197, sd(log combined income te
    mechanism: index.html:7840 `_catchIncome(cell)` returns a single household- and decay-weighted mean income for the whole catchment, which is then fed to two curves estimated on HOUSEHOLD-level BLS quintiles. Unit mismatch: sd(log) 
    fix:       Apply all three factors at ZIP grain for internal consistency (the visits sum already iterates per ZIP, so it is nearly free). File it as a correctness cleanup, and do not expect it to help the ranking — measured, it does not.

  CLEARED: CLEARED — 'ranking on revenue rather than modelled EBITDA or enterprise value is the real error' · CLEARED — the literature-derived income floors are wrong or mis-set · CLEARED — income is genuinely anti-correlated with market quality

